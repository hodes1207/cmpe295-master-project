package testScript;

import java.util.ArrayList;

//import org.junit.Test;

//import static org.junit.Assert.*;
import org.ektorp.CouchDbConnector;

import database.MedicalImage;
import database.databaseAPI;

public class DBTest {

    //@Test
	/*public void test() {
		CouchDbConnector db = databaseAPI.getDBConnection("jing");
		//assertNotNull(db);

	}
    
    //Soket read time out error occur, but it's fine when run as normal Java case
   // @Test
	public void RetrieveImageListTest() {

    	ArrayList<MedicalImage> imageList = databaseAPI.RetrieveImageList(131074, false);
    	//assertNotNull(imageList);
    	System.out.println("image list size: "+imageList.size());
    	
    	MedicalImage image = imageList.get(0);
    	System.out.println("image 1 domainId: "+image.getDomainId());
    	System.out.println("image 1 classId: "+image.getClassId());
    	System.out.println("image 1 imageId: "+image.getImageId());
    	//assertNull(image.getFeatureV());
  	
	}*/

}
